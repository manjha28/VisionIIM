import { useState } from 'react';
import { Icon } from '../assets/icons.jsx';
import { stages } from './data';
export default function Roadmap(){const [active,setActive]=useState(0);const stage=stages[active];return <section className="dark-section roadmap"><div className="wide"><span className="pill dark"><i/>END-TO-END ROADMAP</span><h2>One Platform.<br/>Your Complete IIM Journey.</h2><p className="road-sub">Click through the 6 stages of your transition from aspirant to proud IIM student.</p><div className="road-grid"><div className="stage-list">{stages.map((s,i)=><button key={s.key} className={i===active?'active':''} onClick={()=>setActive(i)}><span>{i+1}. {s.key}</span><Icon name="chevron"/></button>)}</div><article className="stage-panel"><span className="outline-pill">{stage.key}</span><h3>{stage.title}</h3><p>{stage.body}</p><h4>KEY ACTIVITIES</h4><ul>{stage.activities.map(a=><li key={a}>{a}</li>)}</ul><hr/><h5>Why it Matters</h5><p className="why">Most aspirants fail not due to lack of hard work, but due to sequence misalignment. We ensure every milestone is rigorously verified before you step into the interview room.</p></article></div></div></section>}
